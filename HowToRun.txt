
docker compose down -v
docker compose up -d --build
docker exec -it parkar_app php artisan key:generate
docker exec -it parkar_app php artisan migrate

 
docker compose exec app php artisan migrate:fresh  //THis drop previous table
docker compose up -d   for regular run


Now check: http://localhost:18080/api/health

AI service should also be reachable at: http://localhost:5001/analyse
