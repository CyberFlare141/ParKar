
docker compose down -v
docker compose up -d --build
docker compose exec app php artisan key:generate

MySQL now uses database-first import from `schema.sql`.
When the `mysql_data` volume is empty, Docker imports it automatically.
For an existing database volume, apply database-first patches from `database/patches` instead of running Laravel migrations.
Example:
Get-Content database\patches\2026_04_10_google_oauth_users.sql | docker compose exec -T mysql mysql -uroot -pParKar -D ParKar

 
If you change `schema.sql`, rebuild the database with:
docker compose down -v
docker compose up -d --build

docker compose up -d   for regular run


Now check: http://localhost:18080/api/health

cd f:\ParKar\ParKar\parkar-ai-service
docker build -t parkar-ai .
docker run -p 5001:5001 parkar-ai

change folder path in line 14
