
docker compose down -v
docker compose up -d --build
docker compose exec app php artisan key:generate

PostgreSQL now uses database-first import from `schema.sql`.
When the `postgres_data` volume is empty, Docker imports it automatically.
If you are switching from an older MySQL setup, remove the old volume with `docker compose down -v` so PostgreSQL can initialize cleanly.

 
If you change `schema.sql`, rebuild the database with:
docker compose down -v
docker compose up -d --build

docker compose up -d   for regular run


Now check: http://localhost:18080/api/health

cd f:\ParKar\ParKar\parkar-ai-service
docker build -t parkar-ai .
docker run -p 5001:5001 parkar-ai

change folder path in line 14
