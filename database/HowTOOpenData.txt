Open:
http://localhost:18081

The database schema is imported automatically from `schema.sql` when you start Docker with a fresh `postgres_data` volume.
If you are switching from an older MySQL setup, recreate the database with `docker compose down -v` before starting again.


Use these login values:
System: PostgreSQL
Server: postgres
Username: postgres
Password: ParKar
Database: ParKar
